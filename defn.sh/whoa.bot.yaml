---
apiVersion: v1
kind: ConfigMap
metadata:
  name: zerotier-whoa-bot
data:
  ZT_NETWORK: abfd31bd47a6f9f5
  ZT_VIP: 10.88.88.89
  ZT_DEST: traefik-proxy.traefik.svc.cluster.local
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: zerotier-whoa-bot
spec:
  selector:
    matchLabels:
      app: zerotier-whoa-bot
  template:
    metadata:
      labels:
        app: zerotier-whoa-bot
    spec:
      volumes:
        - name: zerotier
          hostPath:
            path: /data/zerotier-whoa.bot
            type: Directory
        - name: tun
          hostPath:
            path: /dev/net/tun
            type: CharDevice
        - name: config
          configMap:
            name: zerotier-whoa-bot
      containers:
      - name: zerotier
        image: letfn/zerotier
        imagePullPolicy: Always
        volumeMounts:
         - name: zerotier
           mountPath: /var/lib/zerotier-one
         - name: tun
           mountPath: /dev/net/tun
         - name: config
           mountPath: /data/config
        securityContext:
          capabilities:
            add: ["NET_ADMIN", "NET_RAW", "SYS_ADMIN"]
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: nginx-whoa-bot
data:
  default.conf: |
    server {
        listen 8080;
        server_name whoa.bot;

        location / {
            root /usr/share/nginx/html;
            index index.html index.htm;
        }

        error_page 500 502 503 504 /50x.html;

        location = /50x.html {
            root /usr/share/nginx/html;
        }
    }
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: whoa-bot
spec:
  selector:
    matchLabels:
      app: whoa-bot
  template:
    metadata:
      labels:
        app: whoa-bot
    spec:
      volumes:
        - name: html
          hostPath:
            path: /data/html-whoa.bot
            type: Directory
        - name: nginx
          configMap:
            name: nginx-whoa-bot
      containers:
      - name: nginx
        image: nginx
        imagePullPolicy: Always
        volumeMounts:
         - name: html
           mountPath: /usr/share/nginx/html
         - name: nginx
           mountPath: /etc/nginx/conf.d
        ports:
        - name: "http"
          containerPort: 8080
          protocol: TCP
---
apiVersion: v1
kind: Service
metadata:
  name: whoa-bot
spec:
  ports:
  - name: http
    port: 80
    protocol: TCP
    targetPort: "http"
  selector:
    app: whoa-bot
  type: ClusterIP
---
apiVersion: traefik.containo.us/v1alpha1
kind: IngressRoute
metadata:
  name: whoa-bot
  labels:
    app: whoa-bot
spec:
  entryPoints:
    - https
  routes:
  - match: Host(`whoa.bot`)
    kind: Rule
    services:
    - name: whoa-bot
      port: 80
