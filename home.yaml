apiVersion: apps/v1
kind: DaemonSet
metadata:
  name: home
spec:
  selector:
    matchLabels:
      app: home
  template:
    metadata:
      labels:
        app: home
        org: empire
        class: sofa
    spec:
      volumes:
        - name: docker
          hostPath:
            path: /var/run/docker.sock
            type: Socket
      containers:
      - name: home
        image: registry.eldri.ch/defn/home
        imagePullPolicy: Always
        command: [ "/tini" ]
        args: [ "--", "/service", "sshd" ]
        volumeMounts:
         - name: docker
           mountPath: /docker.sock
        ports:
        - name: ssh
          containerPort: 2222
          protocol: TCP
---
apiVersion: v1
kind: Service
metadata:
  name: home
spec:
  ports:
  - name: ssh
    port: 22
    protocol: TCP
    targetPort: ssh
  selector:
    app: home
  type: LoadBalancer
